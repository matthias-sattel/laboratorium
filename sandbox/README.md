# How to use sandbox command on MacOS

1. Create sb profile, e.g. terminal-sandbox.sb
2. Run a command in the sandbox using the profile, e.g. zsh
````

```
sandbox-exec -f terminal-sandbox.sb zsh
```

